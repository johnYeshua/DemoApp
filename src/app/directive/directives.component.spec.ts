import { DirectivesComponent } from './directives.component';

describe('DirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesComponent();
    expect(directive).toBeTruthy();
  });
});
